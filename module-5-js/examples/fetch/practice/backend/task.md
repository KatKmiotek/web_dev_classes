Use Trivia or Countries API to fetch data.  
Find in their documentation URL you want to use.  
In script.js file update getData function of URL (it currently has no value `const remoteUrl = "";`). 

Let's print it!  
Now open index.html in default browser. You should see backend response with details of query data. This looks like our local JSON isn't?  
